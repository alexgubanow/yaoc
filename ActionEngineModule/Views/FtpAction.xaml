<UserControl x:Class="ActionEngineModule.Views.FtpAction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ActionEngineModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/" xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <StackPanel>
        <Label Content="Host:"/>
        <TextBox Text="{Binding Host}" TextWrapping="Wrap"/>
        <Label Content="Port:"/>
        <TextBox Text="{Binding Port}" TextWrapping="Wrap"/>
        <Label Content="Username:"/>
        <TextBox Text="{Binding Username}" TextWrapping="Wrap"/>
        <Label Content="Password:"/>
        <TextBox Text="{Binding Passwd}" TextWrapping="Wrap"/>
        <Label Content="Upload path:"/>
        <TextBox Text="{Binding UploadPath}" TextWrapping="Wrap"/>
        <Label Content="Destination file name:"/>
        <TextBox Text="{Binding DestinationFileName}" TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
